export interface UserRegistrationTypes {
  name: string;
  email: string;
  password: string;
  repeatPassword: string;
  role: string;
  id: string;
  userVerified: boolean;
  otp: string;
}
